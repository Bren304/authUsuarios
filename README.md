# 🔐 Flask Auth API (JWT + MySQL + SQLAlchemy)

API REST desarrollada en **Flask** con sistema de autenticación basado en **JSON Web Tokens (JWT)**, conexión a base de datos **MySQL** usando **SQLAlchemy**, y configuración por variables de entorno con **python-dotenv**.

---

## 📌 Tecnologías usadas

| Tecnología | Uso |
|------------|-----|
| Flask | Framework principal |
| Flask SQLAlchemy | ORM para la base de datos |
| MySQL + PyMySQL | Base de datos relacional |
| Flask-JWT-Extended | Manejo de autenticación JWT |
| python-dotenv | Cargar variables del archivo `.env` |
| Werkzeug Security | Hash de contraseñas (`generate_password_hash`, `check_password_hash`) |
